{
  "name": "gcp-pca-mock-app",
  "version": "1.0.0",
  "description": "GCP PCA Mock Exam - Node Backend & Gemini AI",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "stop": "echo 'To stop the server, press Ctrl+C in your terminal. If running as a background process, use: kill $(lsof -t -i:3000)'"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.82.0",
    "@google/generative-ai": "^0.24.1",
    "anthropic": "^0.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "openai": "^6.33.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
